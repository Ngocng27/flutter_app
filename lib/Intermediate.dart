import 'package:flutter/material.dart';

class Intermediate extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Intermediate English'),
      ),
      body: Center(
        child: Text('Lesson content for Intermediate English'),
      ),
    );
  }
}
