import 'package:flutter/material.dart';

class Advance extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Advanced English'),
      ),
      body: Center(
        child: Text('Lesson content for Advanced English'),
      ),
    );
  }
}
